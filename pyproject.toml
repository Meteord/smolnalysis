[project]
name = "smolnalysis"
version = "0.1.0"
description = "Interactive open data analysis app for the build small hackathon."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gradio>=4.44,<6",
    "matplotlib>=3.8,<4",
    "pandas>=2.2,<3",
]

[tool.uv]
package = false
