# OpenUI Support in Gradio App (Chatbot Flow)

- Parent list: [task_list.md](task_list.md)
- Parent task: [01-functional-gradio-app.md](01-functional-gradio-app.md)
- Reference: [OpenUI-Lang docs](https://www.openui.com/docs/openui-lang)

## Goal

Extend the Gradio app so users can interact in a chatbot-like UI:

1. User asks a natural-language question.
2. Backend agentic system performs analysis.
3. Backend returns OpenUI-Lang output.
4. Frontend renders the resulting OpenUI components.

## Status

- Progress: Not started
- Owner:
- Target date:

## Scope

- Chat-style interaction in the Gradio frontend.
- Agentic orchestration in backend for data analysis/tool usage.
- Deterministic OpenUI-Lang response contract from backend.
- Frontend rendering layer that maps OpenUI-Lang to OpenUI React components.
- Error fallback rendering when OpenUI-Lang is invalid or partial.

## Checklist

- [ ] Define message schema for user input, agent steps, and OpenUI-Lang output
- [ ] Add chatbot UI in Gradio (history, user prompt input, assistant responses)
- [ ] Implement backend agent loop for analysis and response generation
- [ ] Add OpenUI-Lang validation/parsing before render
- [ ] Render OpenUI React components in frontend from parsed OpenUI-Lang
- [ ] Add fallback text response when component rendering fails
- [ ] Add at least 3 end-to-end example prompts and expected UI results
- [ ] Document architecture and usage in README

## Acceptance Criteria

- A user can submit a question in the chat UI and receive a rendered UI component response.
- At least one analytical query (for example chart request) is returned as OpenUI-Lang and rendered correctly.
- Invalid OpenUI-Lang does not crash the app and produces a user-friendly fallback response.
- Conversation history is preserved during session.

## Notes

- Keep rendering pipeline modular so model outputs can evolve without frontend rewrites.
- Log raw OpenUI-Lang output for debugging and evaluation.
